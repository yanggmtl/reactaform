# ReactaForm Builder

- Overview: visual drag-and-drop builder for authoring definitions without hand-editing JSON.
- Actions: add fields, configure properties/validation, preview live, export JSON.
- Usage: open the Builder, load a template or start blank, tweak fields, export, then feed the JSON to `ReactaForm`.
